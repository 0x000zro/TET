package com.example.domain.model.pyq

/**
 * Controlled verification status for Previous Year Question metadata (Step 15).
 * Distinguishes authenticated, verified past exam questions from unverified community contributions.
 */
enum class PyqVerificationStatus {
    UNVERIFIED,
    VERIFIED
}
